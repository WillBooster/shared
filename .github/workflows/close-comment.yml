name: Add close comment
'on':
  pull_request:
    types:
      - opened
jobs:
  close-comment:
    permissions:
      pull-requests: write
    uses: WillBooster/reusable-workflows/.github/workflows/close-comment.yml@main
