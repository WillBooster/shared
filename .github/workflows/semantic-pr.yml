name: Lint PR title
'on':
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
permissions:
  pull-requests: read
  statuses: write
jobs:
  semantic-pr:
    uses: WillBooster/reusable-workflows/.github/workflows/semantic-pr.yml@main
